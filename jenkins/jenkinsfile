pipeline {
    agent any

    stages {
        stage('unit tests - backend') {
            agent{
                docker{
                    image 'golang:1.22'
                    reuseNode true
                }
            }
            steps {
                 
                    sh 'go test -v./...'
                             }
        }
    }
}
