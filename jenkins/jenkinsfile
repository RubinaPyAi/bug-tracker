pipeline {
    agent any

    stages {
        stage('unit tests - backend') {
            agent{
                docker{
                    image 'snakee/golang-junit:1'
                    reuseNode true
                }
            }
            steps {
                 dir('bugtracker-backend'){
                    sh 'go test -v./...'
                 }
            }
        }
    }
}
